ELEVENLABS_API_KEY=
# Cough/throat-clear detection is currently Scribe-only (keyless — reuses the Scribe
# event tags in transcript.json). The audio-LLM sweep backends are commented out in
# helpers/ai_providers.py; when re-enabling one, restore its lines below.
# AI_PROVIDER=gemini
# GEMINI_API_KEY=    # Gemini provider disabled (see helpers/ai_providers.py)
# OPENAI_API_KEY=    # OpenAI provider disabled (see helpers/ai_providers.py)
